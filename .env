# Copy this file to .env and fill in your real values.
# .env is git-ignored and must never be committed.

# Server
PORT=3000

# MySQL connection (used only by server.js for tracking storage)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=portfolio_tracking
